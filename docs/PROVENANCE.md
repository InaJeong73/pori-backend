# 원본과 개인 개발본

- 원본: https://github.com/Aingthon/Propofol_BE
- 복제 기준: `main` / `5951ffa6867e013e08c9c7edbf8a32863a098298`
- 개인 개발본 분리일: 2026-09-20
- 기존 커밋과 작성자 정보를 변경하지 않았습니다. 기준 커밋 이후의 변경은 개인 개발 기록입니다.
- 일반 GitHub fork가 아닌 독립 저장소입니다. 원본에 push하거나 PR을 생성하는 자동화는 없습니다.
- 원본의 운영용 secrets·webhook·협업자 권한은 가져오지 않았습니다.
- 기존 배포 workflow는 `docs/upstream-workflows/`에 원형으로 보존합니다.

공개 저장소이므로 누구나 열람할 수 있습니다. 원본과 분리했다는 것이 비공개를 의미하지 않습니다. 팀 코드의 저작자와 출처를 유지하며 새 라이선스를 임의로 부여하지 않습니다.

로컬에서는 `origin`이 개인 저장소를 가리키고, 원본 참고용 `upstream`의 push URL을 `DISABLED`로 설정합니다. 이 설정은 해당 로컬 복제본에만 적용됩니다.
